# Firebase Production Readiness Checklist

Generated by static repo inspection only. Nothing here was verified against the live Firebase Console — anything requiring console access is marked **OWNER ACTION**. No application logic was modified to produce this report.

Firebase project (per `.firebaserc` / `firebase.json`): `construction-rfq-itay-20-2eee0` — **single project, no separate dev/staging/prod projects found.**

---

## Platform config files

| Item | Status | Notes |
|---|---|---|
| `android/app/google-services.json` | **BLOCKED** | Not present in working tree. Correctly listed in `.gitignore` (secret file, regenerated via FlutterFire CLI), but that means it cannot be verified from the repo — confirm it exists in every build environment (local + CI) before shipping an Android build. |
| `ios/Runner/GoogleService-Info.plist` | **BLOCKED** | Same as above — gitignored, not present in working tree. Confirm it exists in the Xcode project / CI secrets before an iOS build. |
| `macos/Runner/GoogleService-Info.plist` | **BLOCKED** | Same — gitignored, not present. Only relevant if macOS is a shipping target. |
| `lib/firebase_options.dart` | DONE | Present, generated by FlutterFire CLI, references project `construction-rfq-itay-20-2eee0` for android/ios/macos/web/windows in `firebase.json`. |
| Android `google-services` Gradle plugin | DONE | Applied in `android/app/build.gradle`. |

## Crashlytics

| Item | Status | Notes |
|---|---|---|
| `firebase_crashlytics` package | DONE | `pubspec.yaml` pins `firebase_crashlytics: ^4.3.10`. |
| Crashlytics wired into app init (recordFlutterError / recordError) | **OWNER ACTION** | Not confirmed by this pass — re-check `main.dart` / error zone setup if not already verified in a prior review. |
| Native Crashlytics Gradle plugin (`com.google.firebase.crashlytics`) | **OWNER ACTION** | Not applied in `android/app/build.gradle`. May be unnecessary for the Flutter plugin's default (non-NDK) crash reporting path, but if native/NDK crash symbolication is desired, this plugin + symbol upload must be added. |
| Crashlytics dashboard shows recent events | **OWNER ACTION** | Requires console access — cannot verify from repo. |

## Analytics

| Item | Status | Notes |
|---|---|---|
| `firebase_analytics` package | DONE | `pubspec.yaml` pins `firebase_analytics: ^11.6.0`. |
| Analytics events actually firing / DebugView verification | **OWNER ACTION** | Requires console access. |

## Firestore

| Item | Status | Notes |
|---|---|---|
| `firestore.rules` present and non-trivial | DONE | Rules include org-membership checks, `isPlatformAdmin()`, `emailVerified()` gating for invitation flows — actively maintained (recent commits `c5bf1aa`, `354ff4b`). |
| Rules deployed matching repo `firestore.rules` | **OWNER ACTION** | Cannot confirm the live rules match this file without console/CLI access (`firebase deploy --only firestore:rules` history not visible from repo). |
| Uncommitted local changes to `firestore.rules` | **BLOCKED** | `git status` at session start shows `M firestore.rules` — modified but not committed. Do not treat this file as reviewed/production until it's committed and (re)deployed. |

## Indexes

| Item | Status | Notes |
|---|---|---|
| `firestore.indexes.json` present | DONE | Contains composite indexes for `quoteRequests`, `supplierQuotes`, `catalogProducts`, etc. |
| Indexes deployed / fully built (not just declared) | **OWNER ACTION** | Index *build status* is only visible in the Firebase Console — a declared index with a pending/failed build state will cause query failures in prod. |

## PITR (Point-in-Time Recovery)

| Item | Status | Notes |
|---|---|---|
| PITR enabled on Firestore | **OWNER ACTION** | This is a console/gcloud-only setting, not present in any repo file — cannot be verified statically. |

## Scheduled Export

| Item | Status | Notes |
|---|---|---|
| Scheduled Firestore export (backup) to Cloud Storage | **OWNER ACTION** | No Cloud Scheduler / export config found in repo (no `functions/` directory, no gcloud scheduler config). Must be configured in Console/gcloud and verified independently. |

## Storage

| Item | Status | Notes |
|---|---|---|
| `storage.rules` present | DONE | Default-deny (`allow read, write: if false`) except public read on `catalog/images/**`. Reasonable posture. |
| Storage rules deployed matching repo | **OWNER ACTION** | Cannot confirm without console/CLI access. |

## Auth

| Item | Status | Notes |
|---|---|---|
| Email/password auth wired in app | DONE | `lib/services/auth_service.dart`, `lib/screens/auth/forgot_password_screen.dart`, `register_screen.dart` present. |
| Email verification enforced server-side | DONE (partial) | `firestore.rules` has an explicit `emailVerified()` guard with a comment noting email/password sign-up doesn't prove address ownership, and requires it for invitation-email-based access — good practice already in place. |
| Enabled sign-in providers match product requirements | **OWNER ACTION** | Provider enablement (Email/Password, Google, etc.) is a console setting — cannot verify from repo which providers are actually turned on. |
| Authorized domains list correct for prod hosting domain | **OWNER ACTION** | Console-only setting. |

## Email Templates

| Item | Status | Notes |
|---|---|---|
| Password reset / email verification templates customized (sender name, branding) | **OWNER ACTION** | Firebase Auth email templates are console-only — not in repo. Given `AppConfig.companyLegalName` currently defaults to a placeholder Hebrew beta-label ("צוות בקשות הצעת מחיר (גרסת בטא)") and `AppConfig.supportEmail` defaults to **empty string** (see `lib/config/app_config.dart:66-80`), confirm the auth email templates don't leak the same unfinished branding/no-reply-address to real users before launch. |

## Custom Claims

| Item | Status | Notes |
|---|---|---|
| `platformAdmin` custom claim is the intended authority | DONE (by design) | `lib/services/platform_admin.dart` and `firestore.rules`' `isPlatformAdmin()` both key off `request.auth.token.platformAdmin`. |
| Claim-setting mechanism in repo | **BLOCKED / OWNER ACTION** | No `setCustomUserClaims` call exists anywhere in the repo (no Cloud Functions directory at all). Claims must be set out-of-band via Admin SDK/console script — confirm this process actually exists and who owns it. |
| Bootstrap admin email allowlist | **OWNER ACTION — security-relevant** | `lib/services/platform_admin.dart` ships a client-side `bootstrapEmails` allowlist (`itayamar206@gmail.com`, `admin@admin.com`) described as "UI-only bootstrap until custom claim is assigned." Confirm before prod launch that (a) this list is pruned to only real owner accounts, or removed entirely, and (b) it is genuinely never trusted for Firestore/Storage authorization (only `isPlatformAdmin()` via custom claim is) — verify no other rule or code path trusts email membership in this list for privileged access. |

## Platform Admin

| Item | Status | Notes |
|---|---|---|
| Admin console gating | DONE | `lib/screens/admin/admin_platform_gate.dart`, `admin_console_screen.dart` present; gated by custom claim per above. |
| At least one real platform admin account provisioned with the custom claim in prod | **OWNER ACTION** | Cannot verify without console access. |

## Environment Separation (Dev / Stage / Production)

| Item | Status | Notes |
|---|---|---|
| Build-time environment flag | DONE | `lib/config/app_config.dart` implements `AppEnvironment {dev, staging, prod}` via `--dart-define=APP_ENV=...`, defaulting to `dev`. |
| **Separate Firebase backends per environment** | **BLOCKED** | `.firebaserc` declares only one project alias (`default` → `construction-rfq-itay-20-2eee0`) and `firebase.json` only configures that single project for all Flutter platforms. This means dev/staging/prod builds as currently configured all point at the **same Firestore/Auth/Storage/Hosting backend** — the `APP_ENV` flag only changes an in-app label, not the actual backend. If real environment isolation is required before launch, additional Firebase projects + `firebase_options.dart` targets (or FlutterFire's multi-environment config) need to be created. |
| Local dev reset tooling | DONE | `scripts/reset_firestore_dev.sh`, `lib/dev/reset_firestore_dev_main.dart` exist for local dev data reset — confirm these can never be pointed at the prod project by accident (they should, since there's only one project — see above). |

## Support Email / Company Name

| Item | Status | Notes |
|---|---|---|
| Company legal name configurable | DONE (placeholder) | `AppConfig.companyLegalName` defaults to a Hebrew beta placeholder, overridable via `--dart-define=COMPANY_LEGAL_NAME=...`. **BLOCKED** until a real value is supplied for the production build command. |
| Support email configurable | DONE (placeholder) | `AppConfig.supportEmail` defaults to **empty string** by deliberate design (comment: avoids a fabricated address), overridable via `--dart-define=SUPPORT_EMAIL=...`. **BLOCKED** until a real support inbox exists and is passed at prod build time — with no value set, `AppConfig.hasSupportEmail` is false and the in-app "contact support" flow presumably falls back to a diagnostics-copy flow instead of a real contact path. |

## Additional finding (outside the requested checklist, flagged for visibility)

| Item | Status | Notes |
|---|---|---|
| Android release signing | **BLOCKED** | `android/app/build.gradle`: `release { signingConfig = signingConfigs.debug }` — release builds are signed with the **debug keystore**. This must be replaced with a real release signing config before any Play Store / production Android build. |

---

### Summary

- **DONE**: platform config wiring (firebase_options.dart, plugins, rules/storage files, base env-flag scaffolding, admin claim architecture) is in place and looks well-built.
- **BLOCKED** (must be resolved before prod, verifiable in-repo): local `google-services.json`/plist not present (expected, gitignored — confirm build pipeline supplies them), uncommitted `firestore.rules` changes, single-Firebase-project environment setup (no real dev/stage/prod isolation), empty support email / placeholder company name, Android release signing on the debug keystore, client-side platform-admin bootstrap allowlist needing pruning/removal.
- **OWNER ACTION** (console/gcloud-only, cannot be verified by reading the repo): Crashlytics/Analytics live data, rules & indexes actually deployed and index build status, PITR enabled, scheduled Firestore export configured, Auth provider enablement + authorized domains, Auth email template branding, at least one prod platform-admin account with the claim actually assigned.
