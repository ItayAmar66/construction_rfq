import '../v2_stat_card.dart';

/// KPI/metric tile. [V2StatCard] already implements this role and is used
/// across the dashboards — this is a discoverable alias, not a new widget.
typedef MetricCard = V2StatCard;
