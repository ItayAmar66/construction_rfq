import '../dashboard_section_header.dart';

/// Section title with accent bar + icon. [DashboardSectionHeader] already
/// implements this role — this is a discoverable alias, not a new widget.
typedef SectionHeader = DashboardSectionHeader;
