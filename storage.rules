rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read for imported catalog product images only.
    match /catalog/images/{imageFile} {
      allow read: if true;
      allow write: if false;
    }

    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
