{
  "name": "construction-rfq-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test": "node supplier_quote_eligibility.emulator.test.js && node critical_security_fixes.emulator.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.0",
    "firebase": "^11.0.0"
  }
}
